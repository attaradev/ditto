version: "2"

linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused
    - gosec
    - misspell
  exclusions:
    rules:
      # G304: file path from variable - intentional in dump scheduler and config loading.
      - linters: [gosec]
        text: "G304"
      # Test files are exempt from errcheck (test helpers often ignore cleanup errors).
      - path: "_test\\.go"
        linters: [errcheck]
      # G101: test fixtures use obvious fake credentials, not real secrets.
      - path: "_test\\.go"
        linters: [gosec]
        text: "G101"

formatters:
  enable:
    - gofmt
    - goimports
